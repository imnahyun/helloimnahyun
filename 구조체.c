struct ranking {

	char username[1024];
	int trycount;
	int time;
}typedef ranking;


int main() {
	int count = 10;
	int trycount = count - trycount

		struct player[10] = { username, trycount }  //gpt가 짜준코드.

	printf("너의 이름을 입력하세요.");
	scanf("%s", player1.username);

	printf("유저의 이름:%s, 시도 횟수 %d, 걸린 시간 %d\n", ranking.username, ranking.trycount, rank.time)