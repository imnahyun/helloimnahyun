struct ranking 
{

	char username[1024];
	int trycount;
	int time;
}typedef ranking; //안에 내용을 ranking만 쓰고 사용가능 


int main() {
	int count = 10; //입력횟수
	int trycount = count - trycount //유저 시도한 횟수

		struct player[10] = { username, trycount }  //gpt가 짜준코드.

	printf("너의 이름을 입력하세요.");
	scanf("%s", player1.username);

	printf("유저의 이름:%s, 시도 횟수 %d, 걸린 시간 %d\n", ranking.username, ranking.trycount, rank.time)